var tsm = require('teamcity-service-messages');

var REPORTER = 'BootLint';

module.exports = {
    reportFn: (file, lint, isError, isWarning, errorLocation) => {
        console.log('//ToDo: impliment reportFn');
    }
};